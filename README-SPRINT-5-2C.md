# Sprint 5.2C - Website router loop fix

Gebruik alleen de map `pdn-website-v5`.

Fix:
- redirect-loop op `/pagina` opgelost;
- wildcard rewrite uit `_redirects` verwijderd;
- `/agenda/<id>` blijft werken;
- bestaande vaste URL's blijven werken.
